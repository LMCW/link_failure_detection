#include "pcap.h"
#include "prefix.h"
#include "trie.h"

int main(int argc, char const *argv[])
{
	int ret;
	terminal_UI();
	// prefix_set S;
	// init(&S, 100000);
	// ret = generateSet(&S,16);
	return ret;
}